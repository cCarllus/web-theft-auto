# Committed e2e config. Loaded by `vite --mode e2e` (see playwright.config.ts); takes precedence over the
# local gitignored .env. Games (loader/spawn/etc.) come from src/game-config.tsx, not env.
VITE_STATIC_URL=http://localhost:3001
