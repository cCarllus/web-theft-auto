{
  "name": "@opensa/loaders",
  "version": "0.0.0",
  "private": true,
  "description": "Asset loaders (fetch manifest chunks / read a local install) that resolve game assets into the VFS.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "nx": {
    "tags": [
      "type:engine"
    ]
  }
}
