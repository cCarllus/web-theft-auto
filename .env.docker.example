# Optional: only needed when generating GTA-backed fixtures inside Docker.
# Copy this file to .env.docker, set an absolute host path, then pass
# --env-file .env.docker to the Docker Compose tools command.
GTA_SA_PATH=/absolute/path/to/GTA San Andreas
